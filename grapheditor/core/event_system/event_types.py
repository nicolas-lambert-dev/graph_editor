from enum import Enum


class EventType(Enum):
    """
    Base enum class from which all EventTypes should inherit
    Value used to store events are string, and this is a guard rail to used existing event types.

    """
    pass